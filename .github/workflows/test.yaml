---
name: test
on:
  push:
    branches: [main]
    tags: [v*]
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-go@v3
      with:
        go-version: '1.18.1'

    - uses: aquaproj/aqua-installer@v1.0.0
      with:
        aqua_version: v1.14.1

    - run: go test ./... -race -covermode=atomic
    - run: golangci-lint run
